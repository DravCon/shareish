# Shareish backend deploy - at repo root so Railway finds this and uses Dockerfile
[build]
builder = "DOCKERFILE"

[deploy]
# Use start.py so PORT from Railway is read in-process (their runner doesn't expand $PORT)
startCommand = "python start.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
